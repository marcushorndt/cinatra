{
  "name": "@cinatra-ai/cli",
  "description": "Internal, monorepo-owned dev/CI extension tooling (dev-extension sync + prod-extension acquisition helpers). The user-facing `cinatra` CLI now ships as the published @cinatra-ai/cinatra package; this package carries ONLY the checkout-operating helper modules the host's dev/release scripts import in-repo (cinatra#402 P2).",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest"
  },
  "dependencies": {
    "tar": "^7.5.16"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
