{
  "name": "@cinatra-ai/streams",
  "description": "Neutral, vocabulary-free stream primitives: a durable per-id Redis-Streams event log, a resumable per-connection SSE wrapper, and a generalized short-lived opaque token broker. The host injects the key-namespace and storage/config accessors; the package carries no AG-UI/A2A vocabulary and no host DB coupling.",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./event-log": "./src/event-log.ts",
    "./sse": "./src/sse.ts",
    "./token-broker": "./src/token-broker.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "ioredis": "^5.11.1",
    "resumable-stream": "^2.2.12"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
