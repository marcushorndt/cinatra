{
  "name": "@cinatra-ai/marketplace-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Catalog sync worker that maps registry package metadata and reconciles them into the Cinatra marketplace catalog.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mapper": "./src/package-mapper.ts",
    "./worker": "./src/sync-worker.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@cinatra-ai/marketplace-mcp-client": "workspace:*",
    "@cinatra-ai/registries": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
