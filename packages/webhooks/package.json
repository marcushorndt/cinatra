{
  "name": "@cinatra-ai/webhooks",
  "description": "Reusable inbound-webhook facility: Standard-Webhooks verify/sign primitives, the generated handler registry, the leased idempotency state machine, and the per-webhook/per-site secret-service contract.",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "standardwebhooks": "1.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
