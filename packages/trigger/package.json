{
  "name": "@cinatra-ai/trigger",
  "description": "MCP primitives for managing agent-run triggers (immediate, scheduled, or recurring) via the actor-aware trigger service.",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@cinatra-ai/agents": "workspace:*",
    "@cinatra-ai/mcp-client": "workspace:*",
    "@cinatra-ai/mcp-server": "workspace:*",
    "zod": "^4.4.3"
  }
}
