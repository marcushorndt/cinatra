// Smoke test stub — connector registry not yet built.
import { describe, it, expect } from "vitest";

describe("connectors (stub)", () => {
  it("registerConnector will be importable once the connector registry exists", () => {
    expect(true).toBe(true);
  });
});
