// Stub: the campaign wizard config handler is intentionally absent because its
// backing store is archived. Two wizard API routes still import this file as a
// side-effect; those routes return 404 for "campaign" resource type until the
// handler is re-implemented. This stub satisfies the TypeScript module reference.
