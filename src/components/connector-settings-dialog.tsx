"use client";

// ConnectorSettingsDialog now lives in @cinatra-ai/sdk-ui so extension settings
// pages can consume it without an `@/` host edge. Re-exported for host callers.
export { ConnectorSettingsDialog } from "@cinatra-ai/sdk-ui";
