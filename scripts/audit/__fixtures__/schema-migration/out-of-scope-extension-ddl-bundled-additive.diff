diff --git a/src/lib/drizzle-store.ts b/src/lib/drizzle-store.ts
--- a/src/lib/drizzle-store.ts
+++ b/src/lib/drizzle-store.ts
@@ -1050,6 +1050,7 @@
     )` },
     { text: `CREATE INDEX IF NOT EXISTS agent_runs_template_id_idx ON "${schemaName.replaceAll('"', '""')}"."agent_runs" (template_id)` },
     { text: `CREATE INDEX IF NOT EXISTS agent_runs_status_idx ON "${schemaName.replaceAll('"', '""')}"."agent_runs" (status)` },
+    { text: `CREATE INDEX IF NOT EXISTS agent_runs_run_by_idx ON "${schemaName.replaceAll('"', '""')}"."agent_runs" (run_by) WHERE run_by IS NOT NULL` },
     // planned_actions and review_tasks tables are absent; synthetic IDs are used.
     // audit_events for @cinatra/authz: structured authorization audit log.
     // Full authorization-audit column set, all fields nullable except id (PK) and created_at.
diff --git a/src/lib/extension-migration-host.ts b/src/lib/extension-migration-host.ts
--- a/src/lib/extension-migration-host.ts
+++ b/src/lib/extension-migration-host.ts
@@ -27,6 +27,11 @@
 // Rollback: the host never migrates extensions down (install/boot/activate
 // only need `up`). The shared runner's per-namespace down fence ships, and
 // `cinatra db migrate --down --dir <abs> --namespace <ns>` is the operator
 // escape hatch for reverting an extension's newest ledger rows.
+//
+// An extension migration module may carry destructive SQL over its OWN
+// tables, e.g.:
+//
+//   pgm.sql(`ALTER TABLE ext_acme_crm_leads DROP COLUMN legacy_score;`);
 
 import {
   extensionMigrationNamespace,
