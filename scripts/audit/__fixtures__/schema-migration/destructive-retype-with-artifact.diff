diff --git a/migrations/core/core__0012_narrow-usage-events-cost-usd.mjs b/migrations/core/core__0012_narrow-usage-events-cost-usd.mjs
new file mode 100644
--- /dev/null
+++ b/migrations/core/core__0012_narrow-usage-events-cost-usd.mjs
@@ -0,0 +1,16 @@
+// core__0012 — narrow usage_events.cost_usd from numeric(12,8) to numeric(12,4).
+// The extra scale was never populated (ingest rounds to 4 decimal places)
+// and the narrower type matches the rollup views. Safe to re-run: converting
+// a column to the type it already has succeeds as a no-op.
+
+/** @param {import("node-pg-migrate").MigrationBuilder} pgm */
+export function up(pgm) {
+  pgm.sql(`ALTER TABLE usage_events
+  ALTER COLUMN cost_usd TYPE numeric(12,4);`);
+}
+
+/** @param {import("node-pg-migrate").MigrationBuilder} pgm */
+export function down(pgm) {
+  pgm.sql(`ALTER TABLE usage_events
+  ALTER COLUMN cost_usd TYPE numeric(12,8);`);
+}
diff --git a/migrations/manifest.json b/migrations/manifest.json
--- a/migrations/manifest.json
+++ b/migrations/manifest.json
@@ -96,6 +96,13 @@
       "seq": "0011",
       "file": "core/core__0011_webhook-legacy-secret.mjs",
       "summary": "Add the legacy single-shared-secret STORAGE columns (legacy_secret_ciphertext/iv) to webhook_secret_bindings for the #343 legacy webhook bridge (D3c option A) deferred by core__0009. A binding flagged legacy_enabled keeps its in-field sender's bespoke `sha256=<hex>` HMAC (the deployed WordPress plugin) while still routing through the generic /webhook facility; the shared HMAC secret is stored ENCRYPTED via the host secretsCodec (ciphertext+iv blob, NOT a ref) under a field-scoped AAD (webhook-binding.<binding_id>.legacy) so a legacy blob can never decrypt in the current/previous columns. Both columns nullable — a Standard-Webhooks binding (the forward default) leaves them null. ADDITIVE + idempotent (ADD COLUMN IF NOT EXISTS) — rides the idempotent bootstrap DDL (buildCreateStoreSchemaQueries adds the same columns); no-op on a bootstrap-seeded schema, ledger-faked on a fresh install. down() drops the two columns.",
       "destructive": false,
       "tables": ["webhook_secret_bindings"]
+    },
+    {
+      "seq": "0012",
+      "file": "core/core__0012_narrow-usage-events-cost-usd.mjs",
+      "summary": "Narrow usage_events.cost_usd numeric(12,8) -> numeric(12,4); scale beyond 4 was never populated.",
+      "destructive": true,
+      "tables": ["usage_events"]
     }
   ]
 }
diff --git a/src/lib/drizzle-store.ts b/src/lib/drizzle-store.ts
--- a/src/lib/drizzle-store.ts
+++ b/src/lib/drizzle-store.ts
@@ -884,7 +884,7 @@ END $$` },
       cached_input_tokens integer NOT NULL DEFAULT 0,
       reasoning_output_tokens integer NOT NULL DEFAULT 0,
       credits_consumed integer NOT NULL DEFAULT 0,
-      cost_usd numeric(12,8),
+      cost_usd numeric(12,4),
       idempotency_key text NOT NULL
     )` },
     { text: `CREATE UNIQUE INDEX IF NOT EXISTS usage_events_idempotency_key_idx ON "${schemaName.replaceAll('"', '""')}"."usage_events" (idempotency_key)` },
