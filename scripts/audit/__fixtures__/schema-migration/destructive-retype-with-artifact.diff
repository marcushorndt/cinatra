diff --git a/migrations/core/core__0010_narrow-usage-events-cost-usd.mjs b/migrations/core/core__0010_narrow-usage-events-cost-usd.mjs
new file mode 100644
--- /dev/null
+++ b/migrations/core/core__0010_narrow-usage-events-cost-usd.mjs
@@ -0,0 +1,16 @@
+// core__0010 — narrow usage_events.cost_usd from numeric(12,8) to numeric(12,4).
+// The extra scale was never populated (ingest rounds to 4 decimal places)
+// and the narrower type matches the rollup views. Safe to re-run: converting
+// a column to the type it already has succeeds as a no-op.
+
+/** @param {import("node-pg-migrate").MigrationBuilder} pgm */
+export function up(pgm) {
+  pgm.sql(`ALTER TABLE usage_events
+  ALTER COLUMN cost_usd TYPE numeric(12,4);`);
+}
+
+/** @param {import("node-pg-migrate").MigrationBuilder} pgm */
+export function down(pgm) {
+  pgm.sql(`ALTER TABLE usage_events
+  ALTER COLUMN cost_usd TYPE numeric(12,8);`);
+}
diff --git a/migrations/manifest.json b/migrations/manifest.json
--- a/migrations/manifest.json
+++ b/migrations/manifest.json
@@ -84,6 +84,13 @@
       "summary": "Add the webhook_secret_bindings table for the per-webhook/per-site secret service (cinatra#340): one active row per (vendor, slug, hook, site) holding the Standard-Webhooks secret material ENCRYPTED via the host secretsCodec (current_secret_ciphertext/iv + an optional previous_secret_ciphertext/iv for the bounded dual-secret rotation window, previous_expires_at, rotated_at), resolved by the server-issued opaque binding_id (PRIMARY KEY) — never from the request payload. site_id uuid (connect_sites.site_id space, D1b). A partial UNIQUE index (vendor, slug, hook, site_id) WHERE revoked_at IS NULL enforces at most one active binding per tuple; revoked_at tombstones a binding. legacy_enabled is the structural hook for the #343 legacy single-shared-secret bridge (its legacy-secret STORAGE columns are deferred to #343 per D3c option A; stays false in #340). ADDITIVE (a brand-new table) — rides the idempotent bootstrap DDL; ships as a pure CREATE … IF NOT EXISTS (no-op on a bootstrap-seeded schema, ledger-faked on a fresh install). down() drops the indexes then the table.",
       "destructive": false,
       "tables": ["webhook_secret_bindings"]
+    },
+    {
+      "seq": "0010",
+      "file": "core/core__0010_narrow-usage-events-cost-usd.mjs",
+      "summary": "Narrow usage_events.cost_usd numeric(12,8) -> numeric(12,4); scale beyond 4 was never populated.",
+      "destructive": true,
+      "tables": ["usage_events"]
     }
   ]
 }
diff --git a/src/lib/drizzle-store.ts b/src/lib/drizzle-store.ts
--- a/src/lib/drizzle-store.ts
+++ b/src/lib/drizzle-store.ts
@@ -884,7 +884,7 @@ END $$` },
       cached_input_tokens integer NOT NULL DEFAULT 0,
       reasoning_output_tokens integer NOT NULL DEFAULT 0,
       credits_consumed integer NOT NULL DEFAULT 0,
-      cost_usd numeric(12,8),
+      cost_usd numeric(12,4),
       idempotency_key text NOT NULL
     )` },
     { text: `CREATE UNIQUE INDEX IF NOT EXISTS usage_events_idempotency_key_idx ON "${schemaName.replaceAll('"', '""')}"."usage_events" (idempotency_key)` },
@@ -912,7 +912,7 @@ END $$` },
       ADD COLUMN IF NOT EXISTS cached_input_tokens integer NOT NULL DEFAULT 0,
       ADD COLUMN IF NOT EXISTS reasoning_output_tokens integer NOT NULL DEFAULT 0,
       ADD COLUMN IF NOT EXISTS credits_consumed integer NOT NULL DEFAULT 0,
-      ADD COLUMN IF NOT EXISTS cost_usd numeric(12,8);
+      ADD COLUMN IF NOT EXISTS cost_usd numeric(12,4);
     IF EXISTS (
       SELECT 1 FROM information_schema.columns
       WHERE table_schema = '${schemaName.replaceAll("'", "''")}' AND table_name = 'usage_events' AND column_name = 'payload'
